;==============================================================================
; ■ asm.asm
;------------------------------------------------------------------------------
;   asm.cpp需要的、一定要用汇编写的函数。
;   这个文件能正确嵌入C++程序中已是一个奇迹……
;==============================================================================

;----------------------------------------------------------------------------
; ● NOP
;   再也找不到比这个函数更没用的函数了 :)
;----------------------------------------------------------------------------
global asm_nop
asm_nop:
	nop
	ret
